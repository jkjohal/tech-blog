{{#each blogs as |blog| }}

<div class="card blog" style="width: 35rem;">
  <div class="card-body">
    <h2 class="card-title"><a href="/blog/{{blog.id}}">{{blog.name}}</a></h2>
    <p>Created by {{blog.user.name}} on {{format_date blog.date_created}}</p>
  </div>
</div>

{{/each}}